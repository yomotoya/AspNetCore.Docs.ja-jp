---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: ASP.NET Web Pages の概要 - データの表示 |Microsoft Docs
author: Rick-Anderson
description: このチュートリアルでは、WebMatrix で、データベースを作成する方法と ASP.NET Web Pages (Razor) を使用すると、データベースのデータをページに表示する方法を説明します。 これは、y が前提としています.
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 9158a1f53268daec6e6fbdf003dd73e1d62cc667
ms.sourcegitcommit: 2d3e5422d530203efdaf2014d1d7df31f88d08d0
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 11/05/2018
ms.locfileid: "51021587"
---
<a name="introducing-aspnet-web-pages---displaying-data"></a>ASP.NET Web ページの概要 - データを表示します。
====================
によって[Tom FitzMacken](https://github.com/tfitzmac)

> このチュートリアルでは、WebMatrix で、データベースを作成する方法と ASP.NET Web Pages (Razor) を使用すると、データベースのデータをページに表示する方法を説明します。 を通じてシリーズを完了したと想定して[ASP.NET Web Pages のプログラミングの概要](../introducing-razor-syntax-c.md)します。
> 
> 学習内容。
> 
> - WebMatrix ツールを使用して、データベースとデータベース テーブルを作成する方法。
> - WebMatrix ツールを使用してデータベースにデータを追加する方法。
> - ページに、データベースからデータを表示する方法。
> - ASP.NET Web Pages での SQL コマンドを実行する方法。
> - カスタマイズする方法、`WebGrid`ヘルパー データ表示を変更して、ページングと並べ替えを追加します。
>   
> 
> 説明した機能/テクノロジ:
> 
> - WebMatrix のデータベース ツールです。
> - `WebGrid` ヘルパー。


## <a name="what-youll-build"></a>構築します

前のチュートリアルでは ASP.NET Web ページに導入された (*.cshtml*ファイル)、Razor の構文の基本およびヘルパー。 このチュートリアルでは、残りの系列に使用する実際の web アプリケーションを作成するが始めます。 アプリは、簡単なムービー アプリケーションを表示、追加、変更、および映画に関する情報を削除することができます。

このチュートリアルを完了したら、このページのようなムービーの一覧を表示することができます。

![CSS クラス名に設定するパラメーターが含まれている WebGrid の表示](displaying-data/_static/image1.png)

開始するにはデータベースを作成する必要があります。

## <a name="a-very-brief-introduction-to-databases"></a>データベースを非常に簡単に紹介

このチュートリアルでは、データベースを簡単に述べます概要のみを提供します。 データベースの経験がある場合は、この短いセクションをスキップできます。

データベースに情報を含む 1 つまたは複数のテーブルが含まれている&mdash;などを顧客、注文、および仕入先、またはの学生、教員、クラス、および成績をテーブルします。 構造上、データベース テーブルは、スプレッドシートのようにします。 通常、アドレス帳を想像してください。 アドレス帳のエントリごとに (つまり、各人の) 名、姓、住所、電子メール アドレス、および電話番号などのいくつかの情報があります。

![サンプル データベースのテーブルとして単純なグリッド](displaying-data/_static/image2.png)

(行と呼ばれることもあります*レコード*、列と呼ばれることがありますと*フィールド*)。

ほとんどのデータベース テーブル、テーブルは、顧客番号、アカウントの数などの一意の値を含む列がある必要があります。 この値が、テーブルのと呼ばれる*主キー*テーブル内の各行を識別するために使用するとします。 例では、ID 列は、前の例に示すように、アドレス帳の主キーです。

Web アプリケーションでは、作業の多くは、データベースから情報を読み取ると、ページに表示することで構成されます。 多くの場合もユーザーから情報を収集し、データベースに追加します。 または、データベースに既にあるレコードを変更します。 (すべて取り上げますこの一連のチュートリアルの過程でこれらの操作です。)

データベースの作業は非常に複雑になることができ、専門知識を要求できます。 このチュートリアルのセットが、理解する必要のみ基本的な概念はすべて説明します。

## <a name="creating-a-database"></a>データベースを作成します。

WebMatrix には、簡単にデータベースを作成して、データベースにテーブルを作成するためのツールが含まれています。 (データベースの構造は、データベースと呼ばれます*スキーマ*)。このチュートリアルのセットが 1 つのテーブルを含むデータベースを作成します&mdash;ビデオ。

これをまだ完了していない場合は、WebMatrix を開き、前のチュートリアルで作成した WebPagesMovies サイトを開きます。

左側のウィンドウでをクリックして、**データベース**ワークスペース。

![WebMatrix の Database ワークスペース タブ](displaying-data/_static/image3.png)

データベース関連のタスクを表示するリボン変更します。 リボンで、次のようにクリックします。**新しいデータベース**します。

![WebMatrix リボンの [新しいデータベース] ボタンをクリックします。](displaying-data/_static/image4.png)

WebMatrix には、SQL Server CE データベースが作成されます (、 *.sdf*ファイル)、サイトと同じ名前を持つ&mdash; *WebPagesMovies.sdf*します。 (ここでは、この操作も行いませんがある限り、好きなデザインにファイルを変更することができます、 *.sdf*拡張機能です)。

## <a name="creating-a-table"></a>テーブルを作成します。

リボンで、次のようにクリックします。**新しいテーブル**します。 WebMatrix は、新しいタブで、テーブル デザイナーを開きます。(新しいテーブル オプションを使用できない場合は、左側のツリー ビューで、新しいデータベースが選択されていることを確認)。

!['新しいテーブル' WebMatrix リボンをボタンします。](displaying-data/_static/image5.png)

(ウォーターマークはという「」と入力テーブル名」) の上部にあるテキスト ボックスで、"Movies"を入力します。

![WebMatrix のデータベース デザイナーでテーブル名を入力します。](displaying-data/_static/image6.png)

テーブル名の下のウィンドウでは、個々 の列を定義します。 *映画*テーブルでは、このチュートリアルには、少数の列を作成します: *ID*、*タイトル*、*ジャンル*、および*年*.

**名前**ボックスに、"ID"を入力します。 ここで値を入力するには、新しい列のすべてのコントロールがアクティブにします。

タブ、**データ型**一覧**int**します。この値は、ID 列が整数型 (数値) のデータを含めることを指定します。

> [!NOTE]
> しません名前にしたいずれかを確認してください (あまり) 言うが標準 Windows キーボードのジェスチャを使用して、このグリッド内で移動することができます。 たとえば、フィールド間にタブは、だけ、一覧で項目を選択するために入力を開始し、そのことができます。


過去のタブ、**既定値**ボックス (つまり、空白のままに)。 タブ、**プライマリ キーである** チェック ボックスを選択します。 このオプションにより、データベースを*ID*列は個々 の行を識別するデータが格納されます。 (は、各行が一意の値をその行を検索するのに使用できる ID 列。)

選択、 **Id**オプション。 このオプションは、新しい行ごとに次の連番を自動的に生成することをデータベースに指示します。 (、 **Id**オプションを選択した場合にのみ使用できる、**プライマリ キーである**オプション)。

次のグリッド行をクリックしてまたは現在の行を終了するには、2 回 Tab キーを押します。 いずれかのジェスチャは、現在の行を保存し、[次へ] の 1 つを開始します。 注意、**既定値**列伝える**Null**します。 (Null、既定値は、既定値のいわば。)

新しい定義が完了したら**ID**デザイナーが次の図のようになりますが、列。

![映画テーブルの ID 列を定義した後、WebMatrix データベース デザイナー](displaying-data/_static/image7.png)

次の列を作成するのボックス内をクリックして、**名前**列。 列の"Title"を入力し、 **nvarchar**の**データ型**値。 "Var"一部**nvarchar**データベースにこの列のデータ サイズは、レコード間を異なる場合があります文字列になることを指示します。 ("N"プレフィックスを表すフィールドがすべてアルファベットまたは書記体系の文字データを保持できることを示す「国」など、フィールドは、Unicode データを保持する)。

選択すると**nvarchar**、もう 1 つのボックスは、フィールドの最大長を入力できますが表示されます。 このチュートリアルで使用するムービーのタイトルはれないこと 50 文字より長くを前提としての 50」と入力します。

Skip**既定値**をオフにし、 **Null を許容**オプション。 タイトルがないようなデータベースに入力される映画を許可するデータベースは必要ありません。

完了したら、次の行に移動すると、デザイナーは、次の図のようになります。

![映画テーブルのタイトル列を定義した後、WebMatrix データベース デザイナー](displaying-data/_static/image8.png)

長さを除く"Genre"をという名前の列を作成する手順だけ 30 に設定を繰り返します。

"Year"という名前の別の列を作成します。 データ型では、選択**nchar** (いない**nvarchar**) と長さは 4 に設定します。 年、しようとしている「1995」または「2010」のように 4 桁の数字を使用して、可変サイズの列を必要としないようにします。

完成した設計がどのようにを次に示します。

![映画のテーブルのすべてのフィールドを定義した後、WebMatrix データベース デザイナー](displaying-data/_static/image9.png)

Ctrl キーを押しながら S キーを押すか、クリックして、**保存**クイック アクセス ツールバーのボタンをクリックします。 タブを閉じるには、データベース デザイナーを閉じます。

## <a name="adding-some-example-data"></a>いくつかの例のデータを追加します。

このチュートリアル シリーズの後半では、フォームに新しい映画を入力 ページを作成します。 ただし、ここには、ページに表示できる、いくつかのサンプル データを追加することができます。

**データベース** ワークスペースで、WebMatrix では、ツリーがあることに注意してください、 *.sdf*以前に作成したファイル。 新しいノードを開いて *.sdf*ファイルを開き、**テーブル**ノード。

![ツリーの映画のテーブルを開くと WebMatrix の Database ワークスペース](displaying-data/_static/image10.png)

右クリックし、**映画**ノード選び、**データ**します。 WebMatrix は、グリッドのデータを入力することができますを開き、*映画*テーブル。

![WebMatrix (空) のデータベース エントリ グリッド](displaying-data/_static/image11.png)

をクリックして、**タイトル**列"と Harry 満たす Sally"を入力します。 移動、**ジャンル**(Tab キーを使用することができます) の列「Romantic コメディ」を入力します。 移動、**年**列「1989」を入力します。

![1 つのレコードを WebMatrix でのデータベース エントリ グリッド](displaying-data/_static/image12.png)

WebMatrix と enter キーを押して、新しいムービーを保存します。 注意、 **ID**列が入力されています。

![1 つのレコードと自動生成された ID を持つ WebMatrix でのデータベース エントリ グリッド](displaying-data/_static/image13.png)

別のビデオ (たとえば、「風と共に去り」、「ドラマ」、「1939」) を入力します。 ID 列はもう一度入力されます。

![WebMatrix で 2 つのレコードと、自動生成された Id のデータベース エントリ グリッド](displaying-data/_static/image14.png)

3 番目のビデオ (たとえば、"Ghostbusters"、「コメディ」) を入力します。 試しのままに、**年**列空白し、し、Enter キーを押します。 選択を解除するため、 **Null を許容**オプション、データベースには、エラーが表示されます。

![必要な列の値が空白の場合、「データが無効です」エラーが表示されます。](displaying-data/_static/image15.png)

クリックして**OK**に戻るし、("Ghostbusters"の年は 1984) エントリを修正し、Enter キーを押します。

8 になるまで、いくつかの映画を入力します。 (8 を入力しやすく後でページングを使用します。 多すぎる場合は、ここで少しだけを入力します)。実際のデータは関係ありません。

![いずれかのレコードを WebMatrix でのデータベース エントリ グリッド](displaying-data/_static/image16.png)

エラーを発生させず、すべての映画を入力した場合、ID の値はシーケンシャルです。 ムービーの不完全なレコードを保存しようとすると、ID 番号はシーケンシャルできない可能性があります。 場合は、それでも大丈夫です。 数値は、固有の意味がないし、内で一意であるは重要であるだけです、*映画*テーブル。

データベース デザイナーを含むタブを閉じます。

これで web ページでこのデータの表示にできます。

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a>ページの WebGrid ヘルパーを使用してデータを表示します。

ページにデータを表示することを使用する、`WebGrid`ヘルパー。 このヘルパーは、グリッドまたはテーブル (行および列) での表示を生成します。 後ほど説明するようにできる絞り込み、グリッドに書式設定、およびその他の機能になります。

グリッドを実行するには、数行のコードを記述する必要があります。 これらのいくつかの行は、このチュートリアルでは、データ アクセスのほぼすべてのパターンの一種として使用されます。

> [!NOTE]
> 実際には、ページ上のデータを表示するためのさまざまなオプションがあります。`WebGrid`ヘルパーは、1 つにすぎません。 選択した、このチュートリアルではデータを表示する最も簡単な方法であるとある程度の柔軟性があるためです。 次のチュートリアルのセットでは、複数の「手動」方法 ページで、データを表示する方法をより直接的に制御を提供するデータを操作する方法を確認します。


WebMatrix で左側のウィンドウでをクリックして、**ファイル**ワークスペース。

作成した新しいデータベースが、*アプリ\_データ*フォルダー。 フォルダーが存在しない場合、WebMatrix により、新しいデータベースに作成されます。 (フォルダーがありますが存在してヘルパーをインストールした場合。)

ツリー ビューでは、web サイトのルートを選択します。 Web サイトのルートを選択する必要がありますアプリに新しいファイルを追加する場合がありますそれ以外の場合、\_データ フォルダー。

リボンで、次のようにクリックします。**新規**します。 **ファイルの種類を選択**ボックスで、選択**CSHTML**します。

**名前**ボックスに、新しいページ"Movies.cshtml"の名前します。

!['映画' ページを表示するファイルの種類の選択 ダイアログ ボックス](displaying-data/_static/image17.png)

をクリックして、 **OK**ボタンをクリックします。 WebMatrix は、いくつかスケルトン要素で新しいファイルを開きます。 まず、データベースからデータを取得するためのコードを記述します。 実際にデータを表示するページにマークアップを追加します。

### <a name="writing-the-data-query-code"></a>データのクエリのコードの記述

ページの上部にある間、`@{`と`}`文字は、次のコードを入力します。 (作成と右中かっこの間、このコードを入力してください)。

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

最初の行では、これは常に、まず、データベースで何かを実行する前に、先ほど作成したデータベースを開きます。 わかる、`Database.Open`データベースを開くのメソッド名。 含まれていないことに注意してください *.sdf*名にします。 `Open`メソッド探すことを想定しています、 *.sdf*ファイル (つまり、 *WebPagesMovies.sdf*) と、 *.sdf*ファイルは、 *アプリ\_データ*フォルダー。 (先ほど述べたようにする、*アプリ\_データ*フォルダーが予約されていますこのシナリオでは、ASP.NET は、その名前を推測する場所の 1 つです。)。

参照がという名前の変数に配置されているデータベースが開かれると、`db`します。 (でしたが名前付きものです。)`db`変数がどのデータベースと対話できるようになります。

2 番目の行が実際には、データベースのデータをフェッチを使用して、`Query`メソッド。 このコードの動作に注意してください。、`db`変数が開かれているのデータベースへの参照を、を起動すると、`Query`メソッドを使用して、`db`変数 (`db.Query`)。

クエリ自体は、SQL`Select`ステートメント。 (少しの背景については、SQL では、後の説明を参照してください)。ステートメントでは、`Movies`クエリにテーブルを識別します。 `*`文字は、クエリがテーブルからすべての列を返す必要がありますを指定します。 (でしたもを一覧表示の列に個別に、コンマで区切られた。)

クエリの結果存在する場合は返されで利用できるよう、`selectedData`変数。 ここでも、変数の名前を使用何か。

最後に、3 行目は ASP.NET のインスタンスを使用する、`WebGrid`ヘルパー。 作成する (*をインスタンス化*) を使用して、ヘルパー オブジェクト、`new`キーワードを使用してクエリ結果を渡すと、`selectedData`変数。 新しい`WebGrid`データベース クエリの結果と共に、オブジェクトで可能な`grid`変数。 その結果は、実際に ページで、データを表示するこの後すぐに必要があります。

データベースが開かれたこの段階で、データが完成し、準備した、`WebGrid`ヘルパーがそのデータ。 次に、ページのマークアップを作成することです。

> [!TIP] 
> 
> **構造化照会言語 (SQL)**
> 
> SQL は、データベース内のデータを管理するために、ほとんどのリレーショナル データベースで使用される言語です。 これには、データを取得し、それを更新できるようにして、作成、変更、およびデータベース テーブル内のデータを管理するためのコマンドが含まれます。 SQL は、プログラミング言語 (c#) のように異なります。 SQL と、データベースを指示することとは、データの取得や、タスクを実行する方法を確認するデータベースの仕事です。 一部の SQL コマンドの例とその実行内容を次に示します。
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> 最初の`Select`ステートメントは、すべての列を取得します (で指定された`*`) から、*映画*テーブル。
> 
> 2 番目の`Select`ステートメント内のレコードからの ID、名、および Price 列のフェッチ、*製品*価格列値が 10 以上のテーブル。 [名前] 列の値に基づいてアルファベット順に結果が返されます。 価格条件に一致するレコードがない場合は、空のセットが返されます。
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> このコマンドに新しいレコードを挿入する、*製品*から 1.99「クロワッサン」、「A 不安定な満足」に説明の列と価格を名前列に設定されているテーブル。
> 
> 数値以外の値を指定する場合に、値を単一引用符 (いない二重引用符、c# と同様) で囲むことに注意してください。 テキストまたは日付の値を囲むが周囲の数値ではなくこれらの引用符を使用するとします。
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> このコマンドのレコードの削除、*製品*が 2008 年 1 月 1 日より前で有効期限の日付列が含まれるテーブル。 (コマンドを前提としていますが、*製品*テーブルにはこのような列は、もちろん)。/MM/DD/YYYY の形式で日付がここに入力したが、現在のロケールに使用される形式で入力してください。
> 
> `Insert`と`Delete`コマンドの結果セットを返しません。 代わりに、コマンドの影響を受けたレコードの数を示す数値が返されます。
> 
> これらの操作 (挿入や、レコードを削除する) などの一部は、操作を要求しているプロセスは、データベース内に適切なアクセス許可があります。 実稼働データベースの多くの場合、データベースに接続するときに、ユーザー名とパスワードを指定する必要がある理由です。
> 
> 数十個の SQL コマンドがありますが、ここに表示するコマンドと同様に、パターンに従うすべて。 SQL コマンドを使用して、データベース テーブルを作成、テーブル内のレコードの数をカウント、価格、計算、および多くの操作を実行することができます。


### <a name="adding-markup-to-display-the-data"></a>データを表示するマークアップを追加します。

内で、`<head>`要素のセットの内容、 `<title>` "Movies"する要素。

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

内で、`<body>`ページの要素は、次のコードを追加します。

[!code-html[Main](displaying-data/samples/sample3.html)]

それで完了です。 `grid`変数を作成したときに作成した値は、`WebGrid`前のコード内のオブジェクト。

WebMatrix のツリー ビューでは、ページを右クリックして**ブラウザーで起動**します。 このページのようなものが表示されます。

![映画テーブルからの既定の WebGrid ヘルパーの出力](displaying-data/_static/image18.png)

その列で並べ替える列見出しリンクをクリックします。 組み込まれている機能は、見出しをクリックして並べ替えられる、 **WebGrid**ヘルパー。

`GetHtml`メソッド、その名前からわかるようには、データを表示するマークアップを生成します。 既定で、`GetHtml`メソッドは、HTML を生成します`<table>`要素。 (する場合は、確認できます、レンダリング、ブラウザーでページのソースを調べることで。)

## <a name="modifying-the-look-of-the-grid"></a>グリッドの外観を変更します。

使用して、`WebGrid`行ったように、ヘルパーは簡単ですが、結果の表示はプレーンな。 `WebGrid`ヘルパーがあらゆる種類のオプション データの表示方法を制御することができます。 このチュートリアルで調査する非常に多くがありますが、このセクションがこれらのオプションのいくつかのアイデアを提供します。 いくつか追加のオプションについては、このシリーズの以降のチュートリアルで説明します。

### <a name="specifying-individual-columns-to-display"></a>表示する個々 の列を指定します。

開始するには、特定の列のみを表示することを指定できます。 既定で、説明したように、グリッドに表示されます 4 つの列のすべて、*映画*テーブル。

*Movies.cshtml*ファイルで、置換、`@grid.GetHtml()`を次に追加したマークアップ。

[!code-css[Main](displaying-data/samples/sample4.css)]

ヘルパーを表示する列を言うに含める、`columns`のパラメーター、`GetHtml`メソッドと列のコレクションを渡します。 コレクション内に含めるには、各列を指定します。 含めることで表示する個々 の列を指定する、`grid.Column`オブジェクト、およびデータの列の名前を指定します。 (これらの列は、SQL クエリの結果に含める必要があります:、`WebGrid`ヘルパーは、クエリによって返されたいない列を表示できません)。

起動、 *Movies.cshtml* 、もう一度、今度は次のいずれかのような表示を取得するブラウザーのページ (ID 列が表示されていないことに注意してください)。

![選択した列のみを表示している WebGrid 画面](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a>グリッドの外観の変更

このセットの後のチュートリアルで紹介うちいくつかの列を表示するための他多数のオプションがあります。 ここでは、このセクションでは紹介方法は、グリッド全体のスタイルを設定できます。

内で、`<head>`終了直前に、ページのセクション`</head>`タグは、次の追加`<style>`要素。

[!code-css[Main](displaying-data/samples/sample5.css)]

この CSS マークアップがという名前のクラスを定義`grid`、`head`など。 個別のこれらのスタイル定義を配置することもできます *.css*ファイルを開き、ページにリンクします。 (実際、行いますこの一連のチュートリアルの後半。)確かに物事を簡単にこのチュートリアルでは、データを表示するのと同じページ内で。

受けることができます、`WebGrid`これらのスタイル クラスを使用するヘルパー。 ヘルパーがいくつかのプロパティ (たとえば、 `tableStyle`) ちょうどこの目的の-には、CSS スタイル クラス名を割り当てるし、クラス名には、ヘルパーによって表示されるマークアップの一部としてレンダリングされます。

変更、`grid.GetHtml`マークアップため、it ではこのコードのようになりますの。

[!code-css[Main](displaying-data/samples/sample6.css)]

追加したことは、新`tableStyle`、 `headerStyle`、および`alternatingRowStyle`パラメーターを`GetHtml`メソッド。 これらのパラメーターは、少し前に追加した CSS スタイルの名前に設定されています。

ページを実行し、この時間を検索する前よりも大幅に低下プレーンなグリッドを表示します。

![CSS クラス名に設定するパラメーターが含まれている WebGrid の表示](displaying-data/_static/image20.png)

何を表示する、`GetHtml`生成されたメソッド、ブラウザーでページのソースで検索できます。 ここでは、詳しく説明しませんが、重要な点はのようにパラメーターを指定して`tableStyle`、次のような HTML タグを生成するグリッドの原因とします。

`<table class="grid">`

`<table>`タグの必要があるが、`class`先ほど追加した CSS 規則のいずれかを参照する属性を追加します。 このコードは、基本的なパターンを示します&mdash;ごとに異なるパラメーター、`GetHtml`メソッドにより参照する CSS クラス、メソッド、マークアップと共に生成されます。 CSS クラスを持つことは自由です。

## <a name="adding-paging"></a>ページングの追加

このチュートリアルの最後のタスクとしてグリッドにページングを追加します。 ここでは、すべての映画を一度に表示する問題はありません。 何百もの動画を追加した場合はページの表示が長くなるとします。

ページのコードで作成する行を変更する、`WebGrid`次のコードにオブジェクト。

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

前に、の唯一の違いが追加されていること、`rowsPerPage`パラメーター 3 に設定されています。

ページを実行します。 グリッドには、時間、およびデータベースのムービーからのページのナビゲーション リンクに 3 つの行が表示されます。

![ページングを使用した WebGrid 表示](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a>次回について

次のチュートリアルでは、Razor および c# のコードを使用して、フォーム内のユーザー入力を取得する方法を学習します。 タイトルまたはジャンルで映画を検索できるように、映画のページに検索ボックスを追加します。

## <a name="complete-listing-for-movies-page"></a>ムービー ページ全体を示します

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a>その他のリソース

- [Razor 構文を使用して ASP.NET Web プログラミングの概要](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> [前へ](intro-to-web-pages-programming.md)
> [次へ](form-basics.md)
