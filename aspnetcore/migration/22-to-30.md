---
title: ASP.NET Core から 2.2 ~ 3.0 プレビューを移行します。
author: tdykstra
description: ASP.NET Core 3.0 に、ASP.NET Core 2.2 プロジェクトを移行する方法について説明します。
ms.author: tdykstra
ms.custom: mvc
ms.date: 04/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 59e8fc4da0d2ce5a2a3ab9e4141b84af0da4b771
ms.sourcegitcommit: eb784a68219b4829d8e50c8a334c38d4b94e0cfa
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/22/2019
ms.locfileid: "59982908"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="01749-103">ASP.NET Core 3.0 を 2.2 からの移行します。</span><span class="sxs-lookup"><span data-stu-id="01749-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="01749-104">によって[Scott Addie](https://github.com/scottaddie)と[Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="01749-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="01749-105">この記事では、ASP.NET Core 3.0 に既存の ASP.NET Core 2.2 プロジェクトを更新する方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="01749-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-3.0.md)]

## <a name="update-the-project-file"></a><span data-ttu-id="01749-106">プロジェクト ファイルを更新します。</span><span class="sxs-lookup"><span data-stu-id="01749-106">Update the project file</span></span>

* <span data-ttu-id="01749-107">設定、[ターゲット フレームワーク モニカー (TFM)](/dotnet/standard/frameworks#referring-to-frameworks)に`netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="01749-107">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="01749-108">削除`<PackageReference>`を[Microsoft.AspNetCore.All](xref:fundamentals/metapackage)または[Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app)メタパッケージ。</span><span class="sxs-lookup"><span data-stu-id="01749-108">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="01749-109">更新プログラム、`Version`残りの`<PackageReference>`要素`Microsoft.AspNetCore.*`パッケージを現在のプレビュー (たとえば、 `3.0.0-preview-18579-0053`)。</span><span class="sxs-lookup"><span data-stu-id="01749-109">Update the `Version` on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview-18579-0053`).</span></span>

  <span data-ttu-id="01749-110">パッケージのバージョンが 3.0 がある場合、パッケージは 3.0 で推奨されていませんが可能性がありません。</span><span class="sxs-lookup"><span data-stu-id="01749-110">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="01749-111">これらの多くは、一部の`Microsoft.AspNetCore.App`個別に参照することはできません。</span><span class="sxs-lookup"><span data-stu-id="01749-111">Many of these are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="01749-112">予備の 3.0 で不要になった生成されたパッケージの一覧を表示するには、次を参照してください。 [3.0 での共有フレームワーク アセンブリをパッケージの生成を停止 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756)します。</span><span class="sxs-lookup"><span data-stu-id="01749-112">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span>

* <span data-ttu-id="01749-113">一部のアセンブリはから削除された`Microsoft.AspNetCore.App`2.x と 3.0 の間。</span><span class="sxs-lookup"><span data-stu-id="01749-113">Some assemblies were removed from `Microsoft.AspNetCore.App` between 2.x and 3.0.</span></span> <span data-ttu-id="01749-114">追加する必要があります`<PackageReference>`で挙げたパッケージから Api を使用している場合に項目[Microsoft.AspNetCore.App 3.0 から削除されているアセンブリ (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755)します。</span><span class="sxs-lookup"><span data-stu-id="01749-114">You may need to add `<PackageReference>` items if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="01749-115">たとえば、`Microsoft.EntityFrameworkCore`と`System.Data.SqlClient`はの一部が不要になった`Microsoft.AspNetCore.App`します。</span><span class="sxs-lookup"><span data-stu-id="01749-115">For example, `Microsoft.EntityFrameworkCore` and `System.Data.SqlClient` are no longer part of `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="01749-116">付属するアセンブリの一覧`Microsoft.AspNetCore.App`まだファイナライズされていないし、3.0 の RTM 前に変更されます。</span><span class="sxs-lookup"><span data-stu-id="01749-116">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized yet and will change before 3.0 RTM.</span></span>

* <span data-ttu-id="01749-117">追加[Json.NET サポート](#jsonnet-support)します。</span><span class="sxs-lookup"><span data-stu-id="01749-117">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="01749-118">既定のプロジェクト、[インプロセス ホスティング モデル](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model)ASP.NET Core 3.0 以降。</span><span class="sxs-lookup"><span data-stu-id="01749-118">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="01749-119">削除することが必要に応じて、`<AspNetCoreHostingModel>`プロパティの値がある場合は、プロジェクト ファイルで`InProcess`します。</span><span class="sxs-lookup"><span data-stu-id="01749-119">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="01749-120">Json.NET のサポート</span><span class="sxs-lookup"><span data-stu-id="01749-120">Json.NET support</span></span>

<span data-ttu-id="01749-121">作業の一部として[ASP.NET Core の共有フレームワークを向上させる](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)、 [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) ASP.NET Core の共有フレームワークから削除されました。</span><span class="sxs-lookup"><span data-stu-id="01749-121">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="01749-122">ASP.NET Core 3.0 プロジェクトでは、Json.NET を使用します。</span><span class="sxs-lookup"><span data-stu-id="01749-122">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="01749-123">パッケージ参照を追加[Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)します。</span><span class="sxs-lookup"><span data-stu-id="01749-123">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="01749-124">Update`Startup.ConfigureServices`を呼び出す`AddNewtonsoftJson()`します。</span><span class="sxs-lookup"><span data-stu-id="01749-124">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson()`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="01749-125">Newtonsoft 設定で設定できる`AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="01749-125">Newtonsoft settings can be set with `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="01749-126">MVC のサービスの登録</span><span class="sxs-lookup"><span data-stu-id="01749-126">MVC service registration</span></span>

<span data-ttu-id="01749-127">ASP.NET Core 3.0 には、内での MVC シナリオを登録するための新しいオプションが追加されます。`Startup.ConfigureServices`します。</span><span class="sxs-lookup"><span data-stu-id="01749-127">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="01749-128">次の 3 つの新しい最上位レベルの拡張メソッドが MVC シナリオに関連する`IServiceCollection`利用できます。</span><span class="sxs-lookup"><span data-stu-id="01749-128">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="01749-129">テンプレートを使用して、これらの新しいメソッドの代わりに`UseMvc`します。</span><span class="sxs-lookup"><span data-stu-id="01749-129">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="01749-130">ただし、`AddMvc`以前のリリースで設定されているとおりに動作し続けます。</span><span class="sxs-lookup"><span data-stu-id="01749-130">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="01749-131">次の例では、コント ローラーと API に関連する機能がないビューまたはページのサポートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-131">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="01749-132">API テンプレートでは、このコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="01749-132">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="01749-133">次の例では、コント ローラー、API に関連する機能、およびビューがないページのサポートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-133">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="01749-134">Web アプリケーション (MVC) テンプレートは、このコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="01749-134">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="01749-135">次の例では、Razor ページのサポートと最小限のコント ローラーのサポートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-135">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="01749-136">Web アプリケーション テンプレートは、このコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="01749-136">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="01749-137">新しいメソッドを組み合わせることもできます。</span><span class="sxs-lookup"><span data-stu-id="01749-137">The new methods can also be combined.</span></span> <span data-ttu-id="01749-138">次の例は、呼び出しに相当`AddMvc`ASP.NET Core 2.2 で。</span><span class="sxs-lookup"><span data-stu-id="01749-138">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="01749-139">ルーティングのスタートアップ コードを更新します。</span><span class="sxs-lookup"><span data-stu-id="01749-139">Update routing startup code</span></span>

<span data-ttu-id="01749-140">アプリを呼び出す場合`UseMvc`または`UseSignalR`にアプリを移行[エンドポイントにルーティング](xref:fundamentals/routing)可能な場合。</span><span class="sxs-lookup"><span data-stu-id="01749-140">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="01749-141">MVC の以前のバージョンとの互換性をエンドポイントのルーティング向上のため、しましたした元に戻す変更の一部で ASP.NET Core 2.2 で導入された URL の生成。</span><span class="sxs-lookup"><span data-stu-id="01749-141">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="01749-142">2.2 でエンドポイントのルーティングを使用して問題が発生した場合は、ASP.NET Core 3.0 での機能強化を予想される次の例外。</span><span class="sxs-lookup"><span data-stu-id="01749-142">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="01749-143">アプリを実装する場合`IRouter`継承または`Route`、この時点で移行しないようにしたい場合があります。</span><span class="sxs-lookup"><span data-stu-id="01749-143">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="01749-144">フィードバックを[エンドポイントのルーティングに基づく IRouter 実装の移行を計画する](https://github.com/aspnet/AspNetCore/issues/4221)します。</span><span class="sxs-lookup"><span data-stu-id="01749-144">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="01749-145">アプリに直接アクセスする場合`RouteData.Routers`MVC での内部をこの時点での移行を避けたい場合があります。</span><span class="sxs-lookup"><span data-stu-id="01749-145">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="01749-146">フィードバックを[RouteData.Routers を使用するための移行ガイダンス](https://github.com/aspnet/AspNetCore/issues/9148)します。</span><span class="sxs-lookup"><span data-stu-id="01749-146">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="01749-147">エンドポイントのルーティングは、同じルート パターンの構文と作成機能としてルート パターンをサポート`IRouter`します。</span><span class="sxs-lookup"><span data-stu-id="01749-147">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="01749-148">エンドポイントのルーティングのサポート`IRouteContraint`します。</span><span class="sxs-lookup"><span data-stu-id="01749-148">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="01749-149">エンドポイントのルーティングのサポート`[Route]`、 `[HttpGet]`、およびその他の MVC ルーティングの属性。</span><span class="sxs-lookup"><span data-stu-id="01749-149">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="01749-150">ほとんどのアプリケーションでのみ`Startup`変更が必要です。</span><span class="sxs-lookup"><span data-stu-id="01749-150">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="01749-151">Startup.Configure を移行します。</span><span class="sxs-lookup"><span data-stu-id="01749-151">Migrate Startup.Configure</span></span>

<span data-ttu-id="01749-152">一般的なアドバイスします。</span><span class="sxs-lookup"><span data-stu-id="01749-152">General advice:</span></span>

* <span data-ttu-id="01749-153">追加`UseRouting`します。</span><span class="sxs-lookup"><span data-stu-id="01749-153">Add `UseRouting`.</span></span> 
* <span data-ttu-id="01749-154">アプリを呼び出す場合`UseStaticFiles`、配置`UseStaticFiles`**する前に**`UseRouting`します。</span><span class="sxs-lookup"><span data-stu-id="01749-154">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="01749-155">アプリなどの認証/承認機能を使用する場合`AuthorizePage`または`[Authorize]`への呼び出しを配置`UseAuthentication`と`UseAuthorization`**後**`UseRouting`します。</span><span class="sxs-lookup"><span data-stu-id="01749-155">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="01749-156">アプリで使用する場合[CORS](xref:security/cors)などの機能`[EnableCors]`、配置`UseCors`[次へ]。</span><span class="sxs-lookup"><span data-stu-id="01749-156">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="01749-157">置換`UseMvc`または`UseSignalR`で`UseEndpoints`します。</span><span class="sxs-lookup"><span data-stu-id="01749-157">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="01749-158">次の例に示します`Startup.Configure`で一般的な ASP.NET Core 2.2 アプリ。</span><span class="sxs-lookup"><span data-stu-id="01749-158">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="01749-159">前の更新後`Startup.Configure`コード。</span><span class="sxs-lookup"><span data-stu-id="01749-159">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="01749-160">ミドルウェアのセキュリティ ガイダンス</span><span class="sxs-lookup"><span data-stu-id="01749-160">Security middleware guidance</span></span>

<span data-ttu-id="01749-161">承認し、CORS のサポートが統合される、[ミドルウェア](xref:fundamentals/middleware/index)アプローチします。</span><span class="sxs-lookup"><span data-stu-id="01749-161">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="01749-162">これにより、これらのシナリオで、同じミドルウェアと機能を使用します。</span><span class="sxs-lookup"><span data-stu-id="01749-162">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="01749-163">このリリースで、更新された承認ミドルウェアが提供されており、CORS ミドルウェアは MVC のコント ローラーによって使用される属性を理解できるように強化されています。</span><span class="sxs-lookup"><span data-stu-id="01749-163">An updated authorization middleware is provided in this release, and CORS middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="01749-164">CORS</span><span class="sxs-lookup"><span data-stu-id="01749-164">CORS</span></span>

<span data-ttu-id="01749-165">以前は、CORS は、構成するのには難しい可能性があります。</span><span class="sxs-lookup"><span data-stu-id="01749-165">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="01749-166">ミドルウェアがいくつかの使用の場合を使用していますが、MVC フィルターを使用するものが提供された**せず**ユース ケースを他のミドルウェア。</span><span class="sxs-lookup"><span data-stu-id="01749-166">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="01749-167">ASP.NET Core 3.0 では、CORS を必要とするすべてのアプリがエンドポイントのルーティングと連携して、CORS ミドルウェアを使用することをお勧めします。</span><span class="sxs-lookup"><span data-stu-id="01749-167">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="01749-168">`UseCors` 既定のポリシーに提供できると`[EnableCors]`と`[DisableCors]`属性を使用して、既定のポリシーをオーバーライドする必要な場合。</span><span class="sxs-lookup"><span data-stu-id="01749-168">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="01749-169">次に例を示します。</span><span class="sxs-lookup"><span data-stu-id="01749-169">In the following example:</span></span>

* <span data-ttu-id="01749-170">すべてのエンドポイントで CORS が有効になって、`default`ポリシーという名前です。</span><span class="sxs-lookup"><span data-stu-id="01749-170">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="01749-171">`MyController`クラスで CORS を無効になります、`[DisableCors]`属性。</span><span class="sxs-lookup"><span data-stu-id="01749-171">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="01749-172">承認</span><span class="sxs-lookup"><span data-stu-id="01749-172">Authorization</span></span>

<span data-ttu-id="01749-173">以前のバージョンの ASP.NET Core では、承認のサポートが経由で提供が、`[Authorize]`属性。</span><span class="sxs-lookup"><span data-stu-id="01749-173">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="01749-174">承認ミドルウェアを使用できなかった。</span><span class="sxs-lookup"><span data-stu-id="01749-174">Authorization middleware wasn't available.</span></span> <span data-ttu-id="01749-175">ASP.NET Core 3.0 では、認証ミドルウェアが必要です。</span><span class="sxs-lookup"><span data-stu-id="01749-175">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="01749-176">承認ミドルウェアを配置することをお勧めします。 (`UseAuthentication`) の直前に`UseAuthorization`します。</span><span class="sxs-lookup"><span data-stu-id="01749-176">We recommend placing the authorization middleware (`UseAuthentication`) immediately before `UseAuthorization`.</span></span> <span data-ttu-id="01749-177">オーバーライドできる既定のポリシーで承認ミドルウェアを構成することもできます。</span><span class="sxs-lookup"><span data-stu-id="01749-177">Authorization middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="01749-178">ASP.NET Core 3.0 以降では、`UseAuthorization`で呼び出される`Startup.Configure`、および次`HomeController`ユーザーで、署名が必要です。</span><span class="sxs-lookup"><span data-stu-id="01749-178">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="01749-179">アプリで使用する場合、`AuthorizeFilter`として MVC でのグローバル フィルターをお勧めするポリシーを提供するコードのリファクタリング、`UseAuthorization`ミドルウェア。</span><span class="sxs-lookup"><span data-stu-id="01749-179">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="01749-180">次の例では、すべてに適用するカスタム ポリシーを要求タイミング`UseAuthorization`を呼び出すと、および`HomeController`アプリへのサインイン ユーザーなしへのアクセスを許可。</span><span class="sxs-lookup"><span data-stu-id="01749-180">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="01749-181">ミドルウェアが承認は、framework の特定の情報の承認なく機能します。</span><span class="sxs-lookup"><span data-stu-id="01749-181">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="01749-182">たとえば、[正常性チェック](xref:host-and-deploy/health-checks)承認などの特定に関する知識を持たないが、正常性チェックは、ミドルウェアによって適用される構成可能な承認ポリシーを持つことができます。</span><span class="sxs-lookup"><span data-stu-id="01749-182">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="01749-183">次の例では、`UseAuthorization`せず、既定のポリシーでは、承認の処理が、`/healthz`正常性チェック エンドポイントであるユーザーが必要です、`admin`ロール。</span><span class="sxs-lookup"><span data-stu-id="01749-183">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="01749-184">一部のシナリオは、保護が実装されます。</span><span class="sxs-lookup"><span data-stu-id="01749-184">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="01749-185">`UseEndpoint` ミドルウェアは、認証または CORS ポリシーが不足しているミドルウェアによりスキップされた場合に例外をスローします。</span><span class="sxs-lookup"><span data-stu-id="01749-185">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="01749-186">進行中の構成の誤りに関するその他のフィードバックを提供するアナライザーのサポート。</span><span class="sxs-lookup"><span data-stu-id="01749-186">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="01749-187">SignalR を移行します。</span><span class="sxs-lookup"><span data-stu-id="01749-187">Migrate SignalR</span></span>

<span data-ttu-id="01749-188">SignalR ハブの現在のマッピングが内で生じた`UseEndpoints`します。</span><span class="sxs-lookup"><span data-stu-id="01749-188">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="01749-189">各ハブをマップ`MapHub`します。</span><span class="sxs-lookup"><span data-stu-id="01749-189">Map each hub with `MapHub`.</span></span> <span data-ttu-id="01749-190">以前のバージョンでは、各ハブは明示的に一覧表示します。</span><span class="sxs-lookup"><span data-stu-id="01749-190">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="01749-191">次の例でのサポート、 `ChatHub` SignalR ハブを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-191">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="01749-192">MVC コント ローラーを移行します。</span><span class="sxs-lookup"><span data-stu-id="01749-192">Migrate MVC controllers</span></span>

<span data-ttu-id="01749-193">内でコント ローラーのマッピングを今すぐ行わ`UseEndpoints`します。</span><span class="sxs-lookup"><span data-stu-id="01749-193">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="01749-194">追加`MapControllers`アプリは、属性ルーティングを使用する場合。</span><span class="sxs-lookup"><span data-stu-id="01749-194">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="01749-195">ルーティングには、ASP.NET Core 3.0 以降、多くのフレームワークのサポートが含まれているため属性ルーティングのコント ローラーの追加はオプトインします。</span><span class="sxs-lookup"><span data-stu-id="01749-195">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="01749-196">次の部分を探します。</span><span class="sxs-lookup"><span data-stu-id="01749-196">Replace the following:</span></span>

* <span data-ttu-id="01749-197">`MapRoute` で `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="01749-197">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="01749-198">`MapAreaRoute` で `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="01749-198">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="01749-199">ここでのルーティングには、MVC 以外のサポートが含まれているために、用語は、ことを明確に状態がどのようにこれらのメソッドに変更されました。</span><span class="sxs-lookup"><span data-stu-id="01749-199">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="01749-200">などの規則ルート`MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute`ことが追加された順序で適用されます。</span><span class="sxs-lookup"><span data-stu-id="01749-200">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="01749-201">(領域のルート) などの具体的なルートを最初に配置します。</span><span class="sxs-lookup"><span data-stu-id="01749-201">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="01749-202">次に例を示します。</span><span class="sxs-lookup"><span data-stu-id="01749-202">In the following example:</span></span>

* <span data-ttu-id="01749-203">`MapControllers` コント ローラーの属性ルーティングのサポートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-203">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="01749-204">`MapAreaControllerRoute` 領域内のコント ローラーの規則ルートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-204">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="01749-205">`MapControllerRoute` コント ローラーの従来のルートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-205">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="01749-206">Razor ページを移行します。</span><span class="sxs-lookup"><span data-stu-id="01749-206">Migrate Razor Pages</span></span>

<span data-ttu-id="01749-207">マッピングが現在の Razor ページ内で生じた`UseEndpoints`します。</span><span class="sxs-lookup"><span data-stu-id="01749-207">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="01749-208">追加`MapRazorPages`アプリは、Razor ページを使用する場合。</span><span class="sxs-lookup"><span data-stu-id="01749-208">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="01749-209">エンドポイントのルーティングには、多くのフレームワークのサポートが含まれているため Razor ページを追加するようになりましたはオプトインです。</span><span class="sxs-lookup"><span data-stu-id="01749-209">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="01749-210">次の例では、 `MapRazorPages` Razor ページのサポートを追加します。</span><span class="sxs-lookup"><span data-stu-id="01749-210">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="01749-211">エンドポイントにルーティングせずに MVC を使用します。</span><span class="sxs-lookup"><span data-stu-id="01749-211">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="01749-212">使用して MVC を使用して`UseMvc`または`UseMvcWithDefaultRoute`ASP.NET Core 3.0 では、明示的にする必要があります内でオプトイン`Startup.ConfigureServices`します。</span><span class="sxs-lookup"><span data-stu-id="01749-212">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="01749-213">これは、機能は、MVC が必要かどうかに初期化中に、承認および CORS ミドルウェアに依存できるため、必要です。</span><span class="sxs-lookup"><span data-stu-id="01749-213">This is required because MVC must know whether it can rely on the authorization and CORS middleware during initialization.</span></span> <span data-ttu-id="01749-214">アナライザーは、アプリがサポートされていない構成を使用しようとしたかどうかに警告を用意にされています。</span><span class="sxs-lookup"><span data-stu-id="01749-214">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="01749-215">アプリには、レガシが必要な場合`IRouter`サポートを無効に`EnableEndpointRouting`アプローチを使用して、次のいずれかで`Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="01749-215">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="01749-216">正常性チェックを移行します。</span><span class="sxs-lookup"><span data-stu-id="01749-216">Migrate health checks</span></span>

<span data-ttu-id="01749-217">正常性チェックとして使用できる、*ルーター ware*のエンドポイントにルーティングします。</span><span class="sxs-lookup"><span data-stu-id="01749-217">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="01749-218">追加`MapHealthChecks`正常性を使用するエンドポイントのルーティングを確認します。</span><span class="sxs-lookup"><span data-stu-id="01749-218">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="01749-219">`MapHealthChecks`メソッドのような引数を受け入れる`UseHealthChecks`します。</span><span class="sxs-lookup"><span data-stu-id="01749-219">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="01749-220">使用する利点`MapHealthChecks`経由で`UseHealthChecks`は承認を適用して、一致するポリシーよりも大きい値を細かく制御する機能です。</span><span class="sxs-lookup"><span data-stu-id="01749-220">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="01749-221">次の例では、`MapHealthChecks`で正常性チェック エンドポイントが呼び出されると`/healthz`:</span><span class="sxs-lookup"><span data-stu-id="01749-221">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="01749-222">HostBuilder WebHostBuilder が置き換えられます</span><span class="sxs-lookup"><span data-stu-id="01749-222">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="01749-223">ASP.NET Core 3.0 テンプレートを使用して、[汎用ホスト](xref:fundamentals/host/generic-host)します。</span><span class="sxs-lookup"><span data-stu-id="01749-223">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="01749-224">以前のバージョンが使用される[Web ホスト](xref:fundamentals/host/web-host)します。</span><span class="sxs-lookup"><span data-stu-id="01749-224">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="01749-225">次のコードは生成された ASP.NET Core 3.0 テンプレート`Program`クラス。</span><span class="sxs-lookup"><span data-stu-id="01749-225">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="01749-226">次のコードはテンプレートによって生成された ASP.NET Core 2.2`Program`クラス。</span><span class="sxs-lookup"><span data-stu-id="01749-226">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="01749-227"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> 型であり、3.0 では、`webBuilder`上記のコード サンプルで確認します。</span><span class="sxs-lookup"><span data-stu-id="01749-227"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="01749-228"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> 将来のリリースで非推奨とされ、置き換え`HostBuilder`します。</span><span class="sxs-lookup"><span data-stu-id="01749-228"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="01749-229">最も重要な変更`WebHostBuilder`に`HostBuilder`に[依存関係の注入 (DI)](xref:fundamentals/dependency-injection)します。</span><span class="sxs-lookup"><span data-stu-id="01749-229">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="01749-230">使用する場合`HostBuilder`、のみを挿入できる<xref:Microsoft.Extensions.Configuration.IConfiguration>と<xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment>に`Startup`のコンス トラクター。</span><span class="sxs-lookup"><span data-stu-id="01749-230">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="01749-231">`HostBuilder` DI の制約。</span><span class="sxs-lookup"><span data-stu-id="01749-231">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="01749-232">1 回だけをビルドする DI コンテナーを有効にします。</span><span class="sxs-lookup"><span data-stu-id="01749-232">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="01749-233">シングルトンの複数のインスタンスを解決するように結果のオブジェクト有効期間に関する問題を回避できます。</span><span class="sxs-lookup"><span data-stu-id="01749-233">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="01749-234">SignalR のコードを更新します。</span><span class="sxs-lookup"><span data-stu-id="01749-234">Update SignalR code</span></span>

<span data-ttu-id="01749-235">呼び出す場合`AddJsonProtocol`、コードに置き換えます`AddNewtonsoftJsonProtocol`します。</span><span class="sxs-lookup"><span data-stu-id="01749-235">If you call `AddJsonProtocol`, replace it with `AddNewtonsoftJsonProtocol`.</span></span>

* <span data-ttu-id="01749-236">次の例は前に、と変更後にサーバー コードを示します。</span><span class="sxs-lookup"><span data-stu-id="01749-236">The following examples show server code before and after the change:</span></span>

  ```csharp
  services.AddSignalR(...)
          .AddJsonProtocol(...) // 2.2
  ```

  ```csharp
  services.AddSignalR(...)
          .AddNewtonsoftJsonProtocol(...) // 3.0
  ```

* <span data-ttu-id="01749-237">次の例は前に、と変更後の .NET クライアント コードを示します。</span><span class="sxs-lookup"><span data-stu-id="01749-237">The following examples show .NET client code before and after the change:</span></span>

  ```csharp
  connection = new HubConnectionBuilder()
      .WithUrl(...)
      .AddJsonProtocol(...) // 2.2
      .Build()
  ```

  ```csharp
  connection = new HubConnectionBuilder()
      .WithUrl(...)
      .AddNewtonsoftJsonProtocol(...) // 3.0
      .Build()
  ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="01749-238">ランタイムのコンパイルにオプトインします。</span><span class="sxs-lookup"><span data-stu-id="01749-238">Opt in to runtime compilation</span></span>

<span data-ttu-id="01749-239">3.0 では、ランタイムのコンパイルは、オプトイン シナリオです。</span><span class="sxs-lookup"><span data-stu-id="01749-239">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="01749-240">ランタイムのコンパイルを有効にするのを参照してください。<xref:mvc/views/view-compilation#runtime-compilation>します。</span><span class="sxs-lookup"><span data-stu-id="01749-240">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
